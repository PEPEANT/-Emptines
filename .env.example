# Client socket server endpoint.
# Local development default is http://localhost:3001, so this can stay empty.
# For production, point this to your deployed socket server origin.
VITE_CHAT_SERVER=

# Optional socket server CORS allow-list (server-side, comma-separated).
# Example:
# CORS_ORIGIN=https://reclaim-fps.netlify.app,https://reclaim-fps.vercel.app

# Optional authoritative server tuning.
# PORT=3001
# MAX_ROOM_PLAYERS=120
# MAX_SOCKET_PAYLOAD_BYTES=12000000
# SIM_TICK_RATE_HZ=20
# SNAPSHOT_AOI_RADIUS=64
# SNAPSHOT_MAX_PEERS=24
# DEFAULT_PORTAL_TARGET_URL=https://www.turbosquid.com/...
# HOST_CLAIM_KEY=change-me
